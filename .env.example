# Firebase web config — copy this file to `.env` and fill in the values from
# the Firebase console (Project settings → your web app → "SDK setup and
# configuration" → Config). These are not secrets (they ship in every client),
# but they differ per project, so they live here instead of in source.
#
# Mapping from the firebaseConfig object Firebase shows you:
#   apiKey            -> EXPO_PUBLIC_FIREBASE_API_KEY
#   authDomain        -> EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN
#   projectId         -> EXPO_PUBLIC_FIREBASE_PROJECT_ID
#   storageBucket     -> EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET
#   messagingSenderId -> EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
#   appId             -> EXPO_PUBLIC_FIREBASE_APP_ID

EXPO_PUBLIC_FIREBASE_API_KEY=
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=
EXPO_PUBLIC_FIREBASE_PROJECT_ID=
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
EXPO_PUBLIC_FIREBASE_APP_ID=
